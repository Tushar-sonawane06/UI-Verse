# PR Size Label Configuration
# Labels are defined in the repository; values are rough change thresholds.

size/xs:
  - changed-files:
      - any: ['**/*']
  - all:
      - changed-files:
          - any: ['**/*']
        count:
          - all: true
            total-changes:
              lteq: 10
